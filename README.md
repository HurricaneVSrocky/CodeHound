# CodeHound
CodeHound links your C++ codebase’s static structure with runtime logs for AI-powered root‑cause analysis. It builds a knowledge graph from CMake artifacts, capturing call chains and log points, then maps live errors back to exact code locations—giving LLMs the context to pinpoint not just what failed, but where and why.
